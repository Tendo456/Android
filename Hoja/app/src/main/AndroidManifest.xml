<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hoja">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hoja"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.seleccion"
            android:label="@string/Selleccion"/>
        <activity android:name=".Activity.ISOS"
            android:label="@string/ISOS"/>
        <activity
            android:name=".Activity.vehiculo"
            android:label="@string/Vehiculo_title" />
        <activity
            android:name=".Activity.ruta"
            android:label="@string/Ruta_title" />
        <activity
            android:name=".Activity.Login"
            android:label="@string/Login_title" />
        <activity
            android:name=".Activity.inicio"
            android:label="@string/Inicio_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.hoja_evaluacion"
            android:label="@string/app_name" />
        <activity
            android:name=".Activity.conductor"
            android:label="@string/Conductor_title" />
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/Main_Activity_title" /> <!-- Intent filter to capture System Browser or Authenticator calling back to our app after sign-in -->
        <activity android:name="com.microsoft.identity.client.BrowserTabActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="com.example.hoja"
                    android:path="/c3cZAKQlqMeay2+yxh5GxNDMBKM="
                    android:scheme="msauth"
                    tools:ignore="AppLinkUrlError" />
            </intent-filter>
        </activity>
    </application>

</manifest>